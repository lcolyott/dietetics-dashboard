import React from "react";

interface AccountFormProps {

};

interface AccountFormState {

};

const AccountUpdateForm: React.FunctionComponent<AccountFormProps> = (props) => {
    return (
        <div>
            Account Update Form
        </div>
    );
};

export default AccountUpdateForm;